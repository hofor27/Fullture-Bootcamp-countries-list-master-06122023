import styled from "styled-components";

const Container = styled.div`
  display: flex;
`;

const Country = () => {
  return (
    <Container>
      <h1>Country</h1>
    </Container>
  );
};

export default Country;
